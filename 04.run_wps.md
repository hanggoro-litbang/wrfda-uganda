# Running WRF Pre-processing
## Create Domain (Geogrid)
go to https://jiririchter.github.io/WRFDomainWizard/ \
Follow the steps to create a domain until you get the namelist.wps file as follows:
```console
&share
 wrf_core = 'ARW',
 max_dom = 3,
 start_date = '2026-04-01_00:00:00', '2026-04-01_00:00:00', '2026-04-01_00:00:00',
 end_date   = '2026-04-02_00:00:00', '2026-04-02_00:00:00', '2026-04-02_00:00:00',
 interval_seconds = 10800,
 io_form_geogrid = 2,
 opt_output_from_geogrid_path = '.',
 debug_level = 0,
/

&geogrid
 parent_id         = 1,1,2,
 parent_grid_ratio = 1,3,3,
 i_parent_start    = 1,99,122,
 j_parent_start    = 1,52,117,
 e_we              = 621,607,457,
 e_sn              = 312,298,346,
 geog_data_res     = 'default','default','default',
 dx                = 9000,
 dy                = 9000,
 map_proj          = 'mercator',
 ref_lat           = -2.613,
 ref_lon           = 118.815,
 truelat1          = -2.613,
 truelat2          = 0,
 stand_lon         = 118.815,
 geog_data_path    = '/scratch/wido/uganda/GEOG',
 opt_geogrid_tbl_path = '.',
! ref_x             = ,
! ref_y             = ,
/

&ungrib
 out_format = 'WPS',
 prefix = 'FILE',
/

&metgrid
 fg_name          = 'FILE',
 io_form_metgrid  = 2,
 opt_metgrid_tbl_path = '.',
/
&mod_levs
 press_pa = 201300 , 200100 , 100000 ,
             95000 ,  90000 ,
             85000 ,  80000 ,
             75000 ,  70000 ,
             65000 ,  60000 ,
             55000 ,  50000 ,
             45000 ,  40000 ,
             35000 ,  30000 ,
             25000 ,  20000 ,
             15000 ,  10000 ,
              5000 ,   1000
 /
```
Create Working directory
```console
mkdir -p /scratch/$USER/WDIR/template
cd /scratch/$USER/WDIR/template
```
Copy namelist.wps
```console
cp /scratch/wido/uganda/namelist.wps .
```
Linking necessary files
```console
ln -sf /home/$USER/WRF/WPS-4.6.0/geogrid/geogrid.exe .
ln -sf /home/$USER/WRF/WPS-4.6.0/geogrid/GEOGRID.TBL.ARW GEOGRID.TBL
```
run geogrid (geo_em.d01 and geo_em.d02)
```console
./geogrid.exe
```
## Ungrib
```console
cd /scratch/$USER/WDIR/template
ln -sf /home/$USER/WRF/WPS-4.6.0/ungrib/src/ungrib.exe .
ln -sf /home/$USER/WRF/WPS-4.6.0/ungrib/Variable_Tables/Vtable.GFS Vtable
ln -sf /home/$USER/WRF/WPS-4.6.0/link_grib.csh .
```
Link GFS data from data directory \
File output is GRIBFILE.???
```console
./link_grib.csh /scratch/wido/uganda/20260601_00/gfs.t00z.pgrb2.0p25.f*
```
File output is  FILE:2026-06-??_??
```console
./ungrib.exe
```
## Metgrid
```console
cd /scratch/$USER/WDIR/template
ln -sf /home/$USER/WRF/WPS-4.6.0/metgrid/src/metgrid.exe .
ln -sf /home/$USER/WRF/WPS-4.6.0/metgrid/METGRID.TBL.ARW METGRID.TBL
```
We can use slurm, since metgrid allow us to run in parallel
First we need to create slurm file
```console
vi metgrid.sbatch
```
write text below and save
```console
#!/bin/bash
#SBATCH -J wrf.exe # Job name
#SBATCH -o wrfexe.out # Name of stdout output file
#SBATCH -N 1 # Total number of nodes requested
#SBATCH -n 20 # Total number of mpi tasks #requested
#SBATCH -t 02:30:00 # Run time (hh:mm:ss) - 2.5 hours
#SBATCH -p riset
# Launch MPI-based executable
ml restore intel
mpirun ./metgrid.exe
```
submit the job and type {squeue -u $USER} to check
```console
sbatch metgrid.sbatch
```
File output is met_em.d01* and met_em.d02*
